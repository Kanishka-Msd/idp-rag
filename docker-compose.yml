version: '3.8'

services:
  idp-api:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./storage:/app/storage
      - ./eval:/app/eval
    restart: unless-stopped